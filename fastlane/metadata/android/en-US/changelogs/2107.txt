v0.47.4 — CI Bug Fixes (companion to v0.47.3)
Two latent build/runtime bugs surfaced by the v0.47.3 rebuild: (1) DeveloperSettingsPage testTemperature/testHumidity state vars were bare mutableStateOf(null), inferred as MutableState<Nothing?> — slider callbacks assigning Int? failed to compile. Now correctly typed as mutableStateOf<Int?>(null) matching testWeatherCode above. (2) drawWatercolorTexture's `for (i in 0..12) rng[i * 13 + 5..8]` OOB-read the 100-element texture rng at i=8 (rng[109])...