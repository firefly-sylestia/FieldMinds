• Fixed the floating explore bubble not appearing even with "Display over other apps" allowed — denying the notification permission no longer suppresses the bubble (it only needs the overlay permission), and a restarted process can no longer leave the bubble invisible
• Mood-board zoom upgrade: any zoomed image can now expand to full screen — tap the expand button (or the chip on saved-board tiles) and the image opens in a full-window viewer where pinch-to-zoom and pan feel natural, even for tiny collage tiles
• Detail pages get a slightly rawer hand-torn look — the hero's seeded tear is a touch more uneven, and the whole banner sits at a subtle per-entry tilt
• Starting a new explore while another session is running now asks first — save the new topic for later, or queue the running session (paused, resumable from Home) and start the new one
• Cabinet polish: the Legacy chip now sits last in the filter row and only appears when there are restored FieldMind records, and the bottom nav bar now blends with the active category's page background
• Fixed the floating explore bubble vanishing after clearing app data — it now retries its overlay window on every new explore session instead of staying silently disabled after one failed attach
• FieldMind restores now carry an explicit persisted legacy marker, so restored records stay separated from native Curio captures even if display metadata changes
• FieldMind restore upgraded: legacy observations and notes now preserve structured metadata, species/taxonomy details, weather, location, timing, tags, and supported media; Curio backup carries the imported species catalog
• Restored FieldMind records now live in a separate Legacy Cabinet section with dedicated detail metadata and species cards, keeping native Curio captures unchanged
• Added an always-available Curio-styled FieldMind observation session from the entry menu; timed sessions save as normal Field Notes entries with provenance

• Pastel mode polish: the Spin deck now reads as one pastel story — the hero ticket opens on the same airy pastel family as the peek cards and the spin button, melting into the page wash in light mode
• Pastel colors brightened: card fills open on the full pastel accent (no darkening) and the pastels got a richer saturation, so the shuffle main card no longer looks dimmed in either mode
• Pastel peek cards re-balanced: they now sit a step darker than the main card instead of glowing brighter than it
• The mood-board collage (entry detail + gallery) now reads clearly in pastel mode — its glyphs switch to deep ink / light tint instead of pastel-on-pastel
• Material card blends reworked: every category now gets its own gradient arrangement (a touch of the category color over your device's Material palette) instead of the same look — light mode leads with your palette colors, dark mode stays deep and readable
• The pastel spin button is less white — the light-mode pastel fill keeps its airy color instead of a white cap
• Main card enhancements fully rebuilt (Settings → Appearance → Main card): a diagonal top-lit gradient, a gradient rim-light border with a bevel edge, layered accent-tinted soft shadows, and editorial display typography — each still a separate toggle, default look untouched
• Spin deck now scales to fit any screen — the shuffle deck, cards and spacing compress together with the Category/Filter buttons on short or narrow screens instead of overflowing
• Mood board fix: the small board and the full-screen expanded board now show the exact same tile arrangement (fit + centered), and the watermark pattern scales with the board so it looks identical when expanded
• Paper makeover: every note gets its own unique grain pattern (no more identical texture), the heavy crease streaks are gone, and normal paper is now sharp-edged like a real sheet
• Coffee, red margin and the folded corner are now universal decorations — the same options work on both torn and ruled paper (cleaner picker, no duplicated choices)
• Mood board v2: the small card now shows the MIDDLE of the collage in a tight cropper look, the watermark pattern is identical small vs expanded, and the double-tap zoom never hides behind the caption/quotes
• Page-switch animations are smooth again — no more glitchy bounce when screens transition, and switching bottom tabs now crossfades cleanly instead of sliding
• The explore timer notification now has a Cancel button — end the session quietly from the shade (Done exploring still jumps to write-it-down)
• Leaving the save-entry page always asks first — save / keep editing / discard when you've added something, a simple confirm when it's empty
• Save-page upgrades: drafts autosave as you type (resume them any time), speak-to-text for voice takes (transcribe instead of recording), and custom tags to find captures later — searchable in the Cabinet and shown on every entry
• Paper styles now STACK — combine coffee stains, the folded corner and the red margin however you like, on ruled or torn paper
• The folded dog-ear is redrawn with paper-true shading — a natural ink-toned flap and feathered shadow that reads right on torn slips, pastel sheets and dark mode
• Mood boards: the small board view is now a true center crop — same arrangement as the full-screen view, with art pinned to the top or bottom of the expanded board hidden until you expand
• Dictation that doesn't quit on you — longer silence tolerance and auto re-listen, plus a small mic button on the title and note fields to dictate into either
• Voice notes and text together — add a transcription to a saved voice note anytime from its detail page
• Mood boards get floating quote boxes — hand-placed paper notes inside the board, tap to edit with full text tools (color picker tucked away)
• Double-tap a mood-board image and the whole board — background included — glides to it, centers it, then the image pops; zooming back out snaps shut instead of lagging
• Mood-board quote notes are now draggable — drag a paper note anywhere on the collage and it stays exactly there in the saved entry (positions scale with the board, small view and full-screen alike)
• Double-tapping a mood-board image now swoops: the whole board arcs along a curved path to the tile instead of sliding in a straight line — smooth glide in, snappy snap out
• Entry heroes get a frosted-glass makeover — blurred backdrop, glassy glowing title, and your mood badge right in the frosted Date · Mood · Type bar
• Mood-board zoom is now one-shot and clean: double-tap a photo and the whole board swoops to it, centered with the image popping over it — it stays at that zoom until you tap to close. No pinch/pan gestures anywhere
• Text-size controls simplified — one size button per toolbar (they both did the same thing)
• Mood-board quotes split: the board's Quote chip adds cards floating on the collage, while the Add-quote button below adds separate quote boxes under the board
• Mood-board editor preview now matches the saved card exactly, with a hint to use full-screen editing for precise placement
• Save or share your mood board as a high-resolution PNG — the full board with its background, watermark, collage and quote boxes, crisp even zoomed in
• Hero fix: frosted blur now lives on the date/mood/type card only (with a glassy topic title) — the backdrop stays crisp again
• Mood-board zoom rework: double-tap zooms ONLY the tapped image (the board stays still) — it glides to the center along its arc, and pinch-to-zoom + one-finger pan now refine just that image
• Fixed the mood-board PNG save/share compile errors
• Double-tap the zoomed mood-board image to reset a pinch/pan back to the fit zoom (instead of closing)
• Fix compile errors: frosted hero card layers now live in an explicit Box (matchParentSize + List gradient overload), restored the missing @Composable on the mood-board renderer
• Build fix: PNG export no longer uses the coroutines 1.9-only onCancellation parameter (cancellation-safe resume) and the zoom gesture code relies on the built-in consume member — compiles on every resolved library version
• Build fix: PNG export capture now uses the stdlib suspendCoroutine (no version-dependent onCancellation parameter) — compiles on every coroutines version
• Removed the duplicate mood card below the entry body — the mood now shows once, in the hero's frosted bar
• FieldMind legacy restore: import observations and notes from archive.json or .fieldmind backups into the Cabinet with timestamps, metadata, supported images and a Legacy badge; imports are additive and repeat-safe, and the species catalog travels with Curio backup/restore
• Mood-board stability: dragging images in the centered inline crop no longer resizes or jumps the viewport, and malformed gesture geometry can no longer crash placement with a NaN roundToInt error
• Explore overlay crash fix: service-owned Compose lifecycle cleanup no longer constructs an invalid saved-state restarter on newer AndroidX lifecycle versions
• Build fix: corrected FieldMind archive documentation paths that accidentally opened a nested Kotlin block comment and hid the legacy importer from the compiler
• Detail hero tear polish: the seeded seam now carries a continuous 6–9-wave rhythm with natural noise variation, while the white under-sheet gets a restrained bumpy edge instead of a rigid line
