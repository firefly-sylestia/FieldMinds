<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.AndroidMirror" parent="Theme.AppCompat.NoActionBar">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryContainer</item>
        <item name="colorAccent">@color/primary</item>
        <item name="colorControlNormal">@color/outline</item>
        <item name="colorControlActivated">@color/primary</item>
        <item name="colorControlHighlight">@color/rippleEffect</item>

        <!-- Background and surface -->
        <item name="android:windowBackground">@color/backgroundDark</item>
        <item name="android:colorBackground">@color/backgroundDark</item>

        <!-- Status & navigation bars — edge-to-edge immersive -->
        <item name="android:statusBarColor">@color/surfaceContainerLowest</item>
        <item name="android:navigationBarColor">@color/backgroundDark</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>

        <!-- Text appearance defaults -->
        <item name="android:textColorPrimary">@color/onBackground</item>
        <item name="android:textColorSecondary">@color/onSurfaceVariant</item>
        <item name="android:textColorTertiary">@color/outline</item>
    </style>
</resources>
