package com.curio.app.features

/**
 * Placeholder screens — all 5 funcs that previously lived here have been
 * promoted to their own feature packages with full implementations:
 *
 *   - SettingsScreen          → com.curio.app.features.settings.SettingsScreen
 *   - OnboardingScreen        → com.curio.app.features.onboarding.OnboardingScreen
 *   - ManageCategoriesScreen  → com.curio.app.features.managecategories.ManageCategoriesScreen
 *   - TopicHistoryScreen      → com.curio.app.features.topichistory.TopicHistoryScreen
 *   - LightboxScreen          → com.curio.app.features.lightbox.LightboxScreen
 *
 * (See CURIO_SPEC.md §2 / §11 / §13.2 / §13.4 / §13.5.)
 *
 * This file is intentionally empty so the once-conflicting top-level names
 * no longer shadow the per-feature implementations. Kept as a directory
 * marker for `com.curio.app.features.*`.
 */
